---
name: tool-routing-reminder
globs: "**/*.{ts,tsx,js,jsx,html,css,md}"
alwaysApply: true
---
Before answering: if this needs live repo/browser state, current library docs, or a
"did I miss anything" check against a web standard, use a tool — don't guess.
Full routing rules: /route-to-tools
